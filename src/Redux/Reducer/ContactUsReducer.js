const contactUState = {
  loading: "",
  error: "",
  success: "",
};
export const conactUsReducer = (state = contactUState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
