const carearState = {
  loading: false,
  error: null,
  message: "",
};
export const carearReducer = (state = carearState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
